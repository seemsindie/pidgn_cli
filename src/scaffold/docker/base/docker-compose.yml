services:
  app:
    build: .
    ports:
      - "4000:4000"
    environment:
      HOST: "0.0.0.0"
      PORT: "4000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4000/health"]
      interval: 10s
      timeout: 3s
      retries: 3
